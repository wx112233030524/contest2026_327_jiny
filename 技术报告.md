# FOCPilot 技术报告

> **队伍编号** 327 ｜ **队名** jiny ｜ **赛道** AI 硬件产品创新
> **队员** 独立完成（1 人，全栈：硬件、驱动、应用、AI 集成、文档）
> **作品仓库** open-vela/contest2026_327_jiny
> **MiMo token 用量** ＿＿＿（控制台数据，待补）

---

## 一、要解决的问题

调 FOC（磁场定向控制）电机的工程师有一个共同的痛点：**PI 参数整定靠试凑**。

改一次参数、烧一次板、看一次波形，一轮十几分钟，一个电流环能磨掉一整天。
而且整定依赖经验——同样一台电机，不同用途（云台 / 航模 / 机器人关节）需要的
带宽和响应完全不同，老师傅的"手感"很难传授。

能自动整定的商用方案（如各家的驱动板配套上位机）动辄绑定特定硬件、一套几万块，
小团队和个人开发者基本用不上。

**FOCPilot 把这件事做成了两块，用一块 100ask DShanPi R528 开发板全包：**

| | 做什么 | 怎么做的 |
|---|---|---|
| **看得见** | 实时显示电机 id / iq / 转速波形 | R528 通过 CAN 以 100 Hz 接收驱动板遥测，LVGL 绘制滚动示波器 |
| **算得出** | 算出电流环 / 速度环共 6 个 PI 参数 | 把电机参数交给大模型推理，电流环 4 个经**白名单校验**后从 CAN 下发回驱动板 |

---

## 二、最重要的一条设计决策：AI 不直接控制电机

**大模型会算错——这是事实，不值得赌。** 尤其当输出会变成电机的真实控制参数时，
一个错误的 Ki 可以让电流环震荡、烧驱动管。

所以整定链路里加了一道数学闸门。**电流环的 4 个参数**（Kp_Id / Ki_Id / Kp_Iq /
Ki_Iq）必须落在经典控制理论解算出的理论值 ±50% 区间内，才允许下发：

```
ωc        = 2π × f_sw / 15            电流环带宽（f_sw = 16 kHz）
Kp_理论   = L × ωc                    Ki_理论 = R × ωc
```

四个参数逐一比对，任一越限即整条结果作废，返回 `FOC_TUNE_WHITELIST_FAIL`，
**电机保持原参数不动**。

**速度环的 2 个参数（Kp_Speed / Ki_Speed）当前不在白名单的比对范围内。** 它们
的安全边界靠的是另一条路：喂给模型的提示词里写死了降阶关系
（`foc_ai_tuner.c:110`，速度环带宽 = 电流环带宽 / 10），要求模型按
"速度环 Kp = 电流环 Kp / 10、Ki = 电流环 Ki / 10" 推导。

这两条路的强度不一样，必须讲清楚：电流环是**确定性代码在事后否决**，速度环是
**提示词在事前引导**。前者即使模型完全失控也拦得住；后者依赖模型照做——模型若不
按公式给速度环参数，目前**没有代码会拒绝它**。这是本条设计决策的真实边界，也记在
下面 8.2 节"已知限制"的第 3 条。

这条边界的意义在于分工：

- **AI 做它擅长的**——在约束区间内搜索、按用途偏好权衡（云台要稳、航模要快）
- **确定性代码守安全边界**——电流环的四个参数上，保证不会输出致命值

`app/focscope/foc_ai_tuner.c` 里这条判断是整个项目安全性的支点。它也让"AI 落到
硬件上"这件事有了一个可辩护的形态：不是让模型接管控制，而是让模型当一个
**被约束的顾问**。

---

## 三、系统架构

### 3.1 硬件

```text
┌─────────────────────────┐          ┌──────────────────────┐
│  100ask DShanPi R528    │  CAN     │   STM32G4 驱动板      │
│  (openvela / NuttX)     │ 500kbps  │   (FOC 控制 + 功率级)  │
│                         │◄────────►│                      │
│  · LVGL 示波器 (触摸屏)  │ 0x501/502│   · 三电阻电流采样     │
│  · AI 整定引擎          │ 遥测     │   · FOC 电流/速度环    │
│  · WiFi (RTL8733BS)     │ 0x101-104│   · 真实电机           │
│  · 板载 ai_agent        │ 0x201-204│                      │
└─────────────────────────┘  整定    └──────────────────────┘
            │
            │ HTTPS (OpenAI 兼容格式)
            ▼
      MiMo 大模型 API
```

驱动板侧代码（STM32G4 工程）不在本仓，本仓提供**协议规格书 + 拷贝即用的 HAL
参考实现**，完整复现需自备驱动板。

### 3.2 软件分层

```text
app/focscope/
├── focscope.c            主程序入口：数据源选择 + LVGL 初始化 + Skill 安装
├── scope_ui.c/.h         LVGL 示波器界面（约 2000 行）
├── scope_data_can.c      CAN 采集线程：解析 0x501/0x502 遥测帧
├── scope_data_sim.c      仿真数据源（无硬件也能看 UI）
├── scope_data.h          数据源抽象层
├── wifi_ui.c/.h          WiFi 连接界面
├── foc_ai_tuner.c/.h     ★ AI 整定核心：MiMo 调用 + 白名单校验 + 结果落盘
├── ai_tuner_test.c       AI 整定命令行工具（Skill 调用的入口）
├── ai_tuner_can.c        AI 整定 CAN 服务：监听请求 → 计算 → 回发
├── ai_tuner_ui.c/.h      AI 整定界面
├── foc_agent_skill.c/.h  ★ 运行时 Skill 安装器
├── agent_skill/          ★ Skill 源文件（Markdown）+ 生成脚本
├── focpilot_can_proto.h  ★ 双端共用 CAN 协议头（R528 ↔ STM32 完全一致）
└── FOCPILOT_CAN_PROTOCOL.md  协议规格书（含 STM32 参考实现）
```

**双端共用同一个协议头文件**是一个刻意的约束：R528 和 STM32 各自 include
`focpilot_can_proto.h`，字段偏移、CAN ID、字节序只有一处定义。协议不一致这类
问题在调试期代价极高，用共享头文件把它变成编译期错误。

### 3.3 生产仓库零改动

本作品**没有修改 openvela 的任何生产仓库**（`nuttx` / `apps` / `packages`）。
接入方式是 manifest 的 `<linkfile>`：

```xml
<project path="contest2026_327_jiny" name="contest2026_327_jiny">
  <linkfile src="app/focscope" dest="apps/examples/focscope"/>
</project>
```

`apps/examples/Make.defs` 本身是 `include $(wildcard $(APPDIR)/examples/*/Make.defs)`，
`apps/examples/Kconfig` 由 `mkkconfig.sh` 自动生成——所以新目录放进去就被自动
发现，不需要动 `apps/` 里任何一行。

---

## 四、运行时 Skill：让板载 ai_agent 用自然语言驱动整定

这是本作品对赛道 3.4 "至少落地一个自定义 Skill" 要求的回应，也是**个人认为
最有价值的一段工作**，因为这里踩到并绕过了上游的两个缺陷。

### 4.1 目标

让用户对着板子说 **"帮我整定这台电机，是个云台用的"**，板载 `ai_agent` 自己
完成：确认参数 → 调用整定 → 读回结果 → 用自然语言汇报。

### 4.2 Skill 是什么，放在哪，怎么被触发

**Skill 定义**：一个 Markdown 文件，装在 `/data/agent/skills/` 下。它告诉模型
"什么情况下该用这个能力"（触发场景）和"具体怎么用"（步骤、参数、结果怎么读）。

本作品的 Skill 是 `agent_skill/motor-tuning.md`（下面为节选，完整版见仓库）：

```markdown
# FOC 电机整定
整定 FOC 电流环/速度环 PI 参数。触发词：整定、调参、PI、电流环、速度环、电机调不好。

## When to use
用户要求整定或调整电机参数时，例如：
- "帮我整定一下这台电机"
- "电流环参数调一下"
- "电机抖得厉害，重新整定一下"
不适用：电机不转/报错等硬件故障排查（那应该先看 focscope 的波形和 CAN 链路状态）。

## How to use
1. 确认电机参数（用户没说就按默认值，并在回复里说明用了默认值）
2. 判断用途：0=云台（低带宽高稳定）/ 1=航模（高响应）/ 2=机器人关节（平衡）
3. 用 run_shell 跑一次整定：ai_tuner_test <Rs> <Ld> <Ke> <poles> <usage>
4. 用 read_file 读结果：/data/agent/tune_result.json
5. 按 status 字段决定怎么回复：ok / whitelist_fail / parse_fail / http_fail / no_key

## 安全说明
白名单只校验电流环 4 个参数：超出理论值 ±50% 即拒绝，电机保持原参数。速度环 2 个
参数没有这层校验，只是提示词要求模型按 1/10 降阶推算，所以不要对外宣称"速度环也校验过"。
因此：被拒绝的参数绝不能描述成"已生效"；没校验过的也要说清是没校验的。
```

**触发场景设计**：描述行里明确列出了触发词，并显式写了**不适用**的情况
（硬件故障排查）。后半句很重要——没有它，模型看到"电机"两个字就会去调 Skill，
而电机不转通常是 CAN 链路或硬件问题，走整定是错的方向。

**触发链路**：ai_agent 启动时扫描 `/data/agent/skills/`，取每个文件的标题与描述
注入系统提示词；模型据此判断是否需要读全文，需要时用 `read_file` 读进来，再按
其中的步骤调 `run_shell`。

```text
用户说"帮我整定这台电机"
      ↓
系统提示词里的 Skills 摘要命中触发词
      ↓
read_file agent_skill/motor-tuning.md   ← 读全文
      ↓
run_shell ai_tuner_test 0.5 0.001 0.01 4 0
      ↓
read_file /data/agent/tune_result.json  ← 读结构化结果
      ↓
自然语言汇报（6 个参数 + 用途模式 + 电流环是否通过白名单）
```

### 4.3 Skill 怎么装到板子上

`ai_agent` 的 `skill_loader.c` **只在启动时安装它自己硬编码的 10 个内置 Skill**，
不扫描仓库。所以外部 Skill 必须由我们自己的应用在运行时写进去。

做法是把 Markdown 编进固件：

```text
agent_skill/motor-tuning.md          ← 人写的唯一源文件
        │ python3 agent_skill/gen_c_string.py
        ▼
foc_agent_skill_data.c               ← 自动生成的 C 字符串，编进固件
        │ focscope 启动时调 foc_agent_skill_install()
        ▼
/data/agent/skills/motor-tuning.md   ← 运行时落到板子上
        │ ai_agent 启动时扫描
        ▼
系统提示词的 Skills 摘要 → 模型据此决定要不要读全文
```

安装时机放在 `focscope` 的 `main()` 里而不是 `ai_tuner_can` 里，是为了避开一个
**鸡生蛋问题**：agent 必须先读到 Skill 才知道该怎么请求整定，所以 Skill 的安装
不能依赖整定程序被调用。`ai_tuner_can` 里也补了一次防御性安装。

每次开机都会比对内容，一致就不重写（避免反复擦写 flash），固件升级带来内容变化
时才更新。

### 4.4 绕过上游缺陷一：描述恒为空

`skill_loader.c` 的 `extract_description()` 从标题行之后开始读，**遇到第一个空行
就 break**：

```c
/* Stop at blank line or section header */
if (len == 0 || (len == 1 && line[0] == '\n') || ...) {
    break;                                    /* ← 空行在这里就断了 */
}

/* Skip leading blank lines */
if (off == 0 && line[0] == '\n') continue;    /* ← 永远到不了：死代码 */
```

而"标题下面空一行"是所有人写 Markdown 的默认习惯（`skill_loader.c` 自己内置的
10 个 Skill 也是这么写的）。结果是**描述恒为空字符串**，触发词永远进不了模型
上下文——Skill 装了，但永远不会被触发，而且**没有任何报错**。

本作品的应对是**标题行下面直接接描述行，中间不留空行**，绕开这个 break。这是
一个针对上游缺陷的适配，代价是排版不符合常规；上游修好后可以改回常规排版
（本作品不影响向后兼容）。

缺陷的完整定位与修复方案已单独整理，准备提 PR 到 `packages/ai_agent`：

```c
if (len == 0 || (len == 1 && line[0] == '\n')) {
    if (off == 0) continue;   /* 还没开始收集：跳过前导空行 */
    break;                    /* 已经开始：这才是描述的结束 */
}
```

### 4.5 绕过上游缺陷二：摘要预算只有 1023 字节

`context_builder.c` 用 **1024 字节的栈缓冲**装全部 Skill 的摘要条目
（`skill_loader_build_summary()`），装不下就**静默丢弃**后面的条目——没有截断
提示、没有日志、没有错误返回。

实测：10 个内置 Skill 占 784 字节，本作品的条目 203 字节，合计 **987 / 1023**，
余量只剩 **36 字节**。再加一个 Skill 就会被挤掉。

这条约束直接决定了 Skill 描述的写法——必须短。它是设计约束，不是实现细节。

**但约束的边界值得说清：进摘要的只有标题行、描述行和文件路径，正文不进去。**
条目格式是 `- **标题**: 描述 (read with: read_file 路径)`，所以**扩写正文不花
预算**，只有改标题/描述/加文件才会。确认这一点之后，Skill 正文就可以写详细
（本作品的 Skill 正文近 3 KB，对摘要毫无影响），而描述必须压到最短。

> **一个反直觉的推论**：正因为缺陷一让内置 Skill 的描述全是空的，它们才只占
> 784 字节。如果上游把缺陷一修好，内置描述全部生效，摘要总量会明显上涨，
> **本作品的条目反而可能被挤掉**。所以本作品在固件里**不**打缺陷一的补丁——
> 修上游和保自己这两件事在短期内是冲突的，这一点在提 PR 时需要向维护者说明。

---

## 五、CAN 协议设计

物理层统一为 **CAN 2.0 标准帧（11-bit ID）、500 kbps、固定 8 字节、小端**。
协议分两组，各自有明确的方向约定。

### 5.1 遥测帧（驱动板 → R528，10 ms / 100 Hz 广播）

**`0x501` 主遥测**——用定点定标而非浮点，省带宽也避免双端浮点格式歧义：

| 字节 | 类型 | 单位 | 说明 |
|---|---|---|---|
| 0-1 | int16 | mA | d 轴电流 id |
| 2-3 | int16 | mA | q 轴电流 iq |
| 4-5 | int16 | rpm | 转子转速 |
| 6-7 | uint16 | mV | 母线电压 Vbus |

**`0x502` 母线电流**：字节 0-3 为 uint32（mA，无符号），字节 4-7 预留。
无专用母线电流传感器时由平均电功率估算（`Ibus = P_avg / Vbus`，再生工况按 0 发）。

### 5.2 整定帧

**请求方向（驱动板 → R528）**——驱动板提出整定请求并上传它知道的电机参数：

| CAN ID | 内容 |
|---|---|
| `0x101` | 整定请求：`data[0]`=cmd、`data[1]`=用途模式、`data[2]`=保留 |
| `0x102` | Rs / Ld（float32 × 2） |
| `0x103` | Lq / Ke（float32 × 2） |
| `0x104` | 极对数（u8）；`data[1]` 保留 |

**结果方向（R528 → 驱动板）**——状态帧 + 三个 PI 参数帧：

| CAN ID | 内容 |
|---|---|
| `0x201` | 整定状态（`FOC_TUNE_*`） |
| `0x202` | Kp_Id / Ki_Id（float32 × 2） |
| `0x203` | Kp_Iq / Ki_Iq（float32 × 2） |
| `0x204` | Kp_Speed / Ki_Speed（float32 × 2） |

另有 `0x301`（R528 → 驱动板：请求上传电机参数）。

**状态码**是这条协议里安全性的载体——白名单拒绝不是"没回复"，而是一个显式的
`FOC_TUNE_WHITELIST_FAIL`（`0x04`），驱动板据此明确知道"参数没变"：

```c
FOC_TUNE_IDLE           0x00   /* 空闲, 可接收新请求 */
FOC_TUNE_STARTED        0x01   /* 已收到参数, 开始计算 */
FOC_TUNE_OK             0x02   /* 计算完成, PI 参数有效 */
FOC_TUNE_PARAM_INVALID  0x03   /* 电机参数不合法 */
FOC_TUNE_WHITELIST_FAIL 0x04   /* PI 越限, 白名单校验失败 */
FOC_TUNE_AI_FAIL        0x05   /* AI 调用失败 */
FOC_TUNE_BUSY           0x06   /* 正在计算, 拒绝新请求 */
```

参数与结果都分三帧发送而不是一帧全塞，是为了让每帧都落在 8 字节载荷内，
同时便于驱动板侧逐项确认（也留出了新增参数的余地）。

---

## 六、关键排障：CAN 模块时钟不是 40 MHz

这是整个项目里最有代表性的一次排障，也是 AI 协作价值最直接的体现。

**现象**：CAN 完全不通，收发双向都没有任何数据。

**排查路径**：CAN 不通的可能层级有四个——硬件接线 / 收发器、驱动配置、时序参数、
应用层。人工排查通常要在四层之间反复试错，**一轮就是一次编译烧录**（本项目全量
编译约 10 分钟）。

**收敛过程**：从驱动源码、示波器波形记录和串口日志三方对照，逐层排除：

1. 收发器供电与总线电平——正常，排除硬件
2. 驱动是否成功注册、`/dev/can0` 是否出现——正常，排除驱动加载
3. 收发器 120 kΩ / 500 kΩ 终端电阻失配——**发现一处问题并修复**，但修完仍不通
4. 把怀疑收敛到时序参数，用示波器**实测 CAN 模块时钟**

**结论**：R528 的 CAN 模块时钟实测是 **24 MHz，而不是数据手册预期的 40 MHz**。
按 40 MHz 算出来的 500 kbps 时序（brp / tseg1 / tseg2）整体偏了，位定时根本对
不上。按 24 MHz 重算后：

```
brp = 2, tseg1 = 15, tseg2 = 8   →  500 kbps @ 24 MHz
```

改完立即通了。

**为什么值得写进报告**：AI 一开始也是按 40 MHz 算的时序——**它算错了**。纠正它的
不是另一个模型，而是示波器实测。这件事恰好印证了本作品的核心设计立场：
**模型的输出必须经过确定性验证**，PI 参数如此，时钟频率也是如此。报告里保留
这个错误，比宣称"AI 一次做对"更有说服力。

---

## 七、AI 开发过程

本作品从零到真机跑通，全程使用 **Claude Code** 辅助开发，完整对话日志见
`logs/wx112233030524/`（官方采集器导出，schema 1.0；开发持续进行，日志随之更新）。

### 7.1 AI 参与到了哪些环节

| 环节 | AI 做了什么 |
|---|---|
| **需求拆解与选型** | 讨论"示波器 + 整定"的产品形态，共同确定"AI 只出建议值、白名单兜底"的安全架构 |
| **方案设计** | CAN 遥测帧与整定协议设计；双端共用同一头文件的一致性策略 |
| **编码** | 约 2000 行 LVGL 示波器界面、CAN 采集线程、MiMo API 客户端、白名单校验器、运行时 Skill 安装器 |
| **调试验证** | 真机日志分析（CAN 时钟收敛过程见第六节） |
| **死锁排查** | 定位并修复 100ask 版本 LVGL 的 `lvgl_lock()` 死锁（多次加锁导致自锁），修复后 UI 稳定 |
| **上游缺陷分析** | 定位 `extract_description()` 死代码与摘要预算溢出两个缺陷，给出修复方案 |
| **文档** | 协议规格书、模块 README、代码注释 |

### 7.2 独立开发的真实工作量

本项目由一人独立完成，AI 协作在这里的价值不是"替代人"，而是**把人的精力从
样板代码里释放出来**：

- LVGL 界面样板代码密度极高且高度重复，这部分交给 AI 后，精力集中在真正的设计
  决策上（安全架构、协议设计、带宽与响应如何权衡）
- 排障时能同时读懂驱动源码、波形记录和串口日志，快速排除不可能的层级

### 7.3 AI 生成的代码并非照单全收

**必须与硬件一致的地方全部经过真机验证**：

- CAN 时序参数——AI 最初按错误时钟（40 MHz）算出，由示波器实测纠正
- CAN 模块时钟频率——实测确认，不以手册为准
- 协议字段与偏移——双端共用头文件 + 真机收发验证

日志里完整保留了这些往返过程。

---

## 八、完整度与验证情况

### 8.1 已在真机验证

| 项目 | 状态 |
|---|---|
| openvela 编译通过，生成可烧录镜像 | ✅ |
| LVGL 示波器 UI 在 R528 上稳定运行（触屏交互、滚动波形） | ✅ |
| CAN 通信打通，500 kbps 下稳定接收遥测帧 | ✅ 时序经示波器实测校准 |
| WiFi 连接（板载 RTL8733BS） | ✅ |
| MiMo API 调用成功返回 PI 参数，电流环白名单校验生效 | ✅ 填入真实 key 后验证 |
| 运行时 Skill 安装到 `/data/agent/skills/` | ⬜ 待上板确认（演示视频将展示） |

> 关于最后两行：源码中 API key 为占位符 `YOUR_MIMO_API_KEY`（本仓公开，故不提交
> 真实密钥），因此直接编译出的固件跑到 API 调用这步会返回 `no_key` 状态并给出明确
> 提示，而不是静默失败——这是有意设计的默认行为。填入 key 后即走通全链路。
> 运行时 Skill 那一行标为待确认，是因为**安装器与 Skill 文本已编进固件、编译通过，
> 但"装到板子上并被 agent 真正触发"这一步还没有真机跑过**。演示视频会完整展示
> 这一过程；若届时仍未跑通，会按实际情况改为尚未验证，不做模糊表述。

### 8.2 已知限制（如实列出）

1. **驱动板侧代码不在本仓**——STM32G4 工程未开源，本仓提供协议规格书与 HAL 参考
   实现，完整复现需自备驱动板
2. **AI 整定需要联网**——无网络时命令行明确报错退出，不影响示波器功能
3. **速度环参数没有白名单保护**——白名单只比对电流环 4 个参数；Kp_Speed / Ki_Speed
   靠提示词里的降阶公式引导，模型若不照做，当前没有代码拒绝。补齐方式是拿电流环
   校验通过后的值直接算出速度环参数（`foc_ai_tuner.c:110` 已写明该公式），把速度环
   也纳入确定性范围，这是下一步计划的第一项
4. **白名单当前是固定 ±50% 区间**——尚未按电机用途（云台 / 航模 / 机器人关节）
   做差异化收紧
5. **Skill 摘要余量只有 36 字节**——受上游 1023 字节预算限制，再加 Skill 会被挤掉
   （见 4.5 节）

---

## 九、商业潜力

**痛点真实且付费意愿明确**：电机调试是 FOC 产品开发中占比最高的时间成本之一。
商用自动整定方案绑定特定驱动板、价格数万，中小团队与个人开发者被排除在外。

**本作品的可复制性**：

- 代码可低成本移植到任何带 CAN 的 openvela 板子上
- 协议规格书 + 参考实现使驱动板侧接入成本可控
- 不依赖特定厂商的电机或功率级

**可落地的产品形态**：

1. **电机厂商的配套调试工具**——随驱动板出货，降低客户上手门槛
2. **独立的手持整定仪**——面向维修与产线快速标定
3. **SaaS 化的参数服务**——把"电机参数 → PI 参数"的映射沉淀为可复用知识库，
   白名单区间随数据积累逐步收紧

**技术壁垒**：不在于"调用大模型"，而在于**让模型输出可安全落地的那套约束机制**。
白名单校验、结构化结果回传、Skill 的触发边界设计，共同构成了把概率性输出接入
确定性控制回路的工程方法。这套方法可以迁移到任何"AI 建议 → 硬件执行"的场景。

---

## 十、总结

FOCPilot 想证明的不是"大模型能调电机"，而是**大模型可以被安全地用在控制回路
旁边**。

三条具体结论：

1. **模型的输出必须过确定性闸门**——电流环白名单让 AI 的错误不会变成电机的危险
   参数（速度环这块闸门还没装上，见 8.2 第 3 条，本报告不把它算作已完成）；
   示波器让 AI 算错的时钟频率被实测纠正。同一立场，两个场合。
2. **接入要遵守上游的边界**——生产仓库零改动，全部通过 manifest 软链与运行时
   Skill 安装完成；Skill 的装配链路完全走 openvela 既有的 `/data/agent/skills/`
   机制，不引入私有约定。
3. **发现上游缺陷要如实报告**——`extract_description()` 死代码与摘要预算溢出
   两个问题都已定位、给出修复方案，并说明了"修上游"与"保自己"在短期内的冲突，
   准备了 PR 一并提交。

---

*本作品采用 Apache License 2.0 许可。*
